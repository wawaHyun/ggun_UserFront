
export const  LocalNumberOptions: Intl.NumberFormatOptions ={
  // style: "currency",
  currency: "KRW",
 currencyDisplay: "code",
  // maximumFractionDigits: 2,
  // minimumFractionDigits: 2,
  useGrouping: true
};

// use to : number.toLocaleString("ko", LocalNumberOptions);